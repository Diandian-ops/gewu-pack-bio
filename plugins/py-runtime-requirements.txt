# BioF3 Python 运行时基础科学栈 (biof3-py-runtime)
# 与 resources/built-in-plugins/manifest.json 中 biof3-py-runtime 的声明一致。
#
# 仅含「基础运行时」包。FigCode 卡片所需的 squidpy / liana / xgboost / shap /
# scvelo 等由各自的 figcode-* 插件制品单独提供，不在此列，避免膨胀基础包。

numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12

# 单细胞分析基础依赖（manifest 声明含 scanpy/anndata）
anndata>=0.9
scanpy>=1.9

# 通用工具
requests>=2.28
tqdm>=4.65
