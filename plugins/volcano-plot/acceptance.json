{
  "schemaVersion": 1,
  "pluginId": "volcano-plot",
  "pluginVersion": "1.0.0",
  "platforms": ["macos-arm64", "macos-x64", "windows-x64"],
  "fixtures": [
    {
      "id": "valid",
      "description": "12 genes with mix of significant and non-significant",
      "inputs": {
        "deg_table": "demo-data/demo_deg.csv",
        "gene_col": "gene",
        "lfc_col": "log2FoldChange",
        "pval_col": "padj",
        "fc_threshold": 1,
        "pval_threshold": 1.3
      },
      "expectedStatus": "valid",
      "expectedOutputs": ["volcano.png", "classified_table.csv", "report.html"]
    },
    {
      "id": "empty-result",
      "description": "No significant genes — volcano plot still rendered",
      "inputs": {
        "deg_table": "demo-data/demo_deg.csv",
        "gene_col": "gene",
        "lfc_col": "log2FoldChange",
        "pval_col": "padj",
        "fc_threshold": 5,
        "pval_threshold": 3
      },
      "expectedStatus": "valid",
      "expectedNote": "No significant genes highlighted — valid empty visual result"
    },
    {
      "id": "log2fc-alias",
      "description": "DESeq2 direct output with log2FC column — auto-detection",
      "inputs": {
        "deg_table": "demo-data/demo_deg.csv",
        "pval_col": "padj"
      },
      "expectedStatus": "valid",
      "expectedNote": "lfc_col auto-detected as log2FC from demo_deg.csv"
    },
    {
      "id": "explicit-raw-pvalue",
      "description": "Explicit raw pvalue remains available as an intentional scientific change",
      "inputs": {
        "deg_table": "demo-data/demo_deg.csv",
        "pval_col": "pvalue"
      },
      "expectedStatus": "valid",
      "expectedNote": "Output records significance_column=pvalue and preserves padj separately"
    },
    {
      "id": "missing-inputs",
      "description": "No DEG file provided",
      "inputs": {},
      "expectedStatus": "blocked",
      "expectedFailureMode": "missing_required_file_inputs"
    }
  ],
  "reproducibility": {
    "requireScriptSha256": true,
    "requireParamsHash": true,
    "requireInputSha256": true,
    "requireOutputSha256": true,
    "requireRVersion": true,
    "requirePackageVersions": true,
    "evidenceSource": "runtime-generated artifact-manifest.json + provenance.json"
  }
}
